#include "compute.h"


namespace compute {
	int add_two(int num)
	{
		return num + 2;
	}

	void testGetInput() {
		string strName;

		cout << "test getFromInput()" << "\n";

		// Get Parameter from user
		//do {
		//	getFromInput("Enter your name", strName);
		//} while (strName.empty());
	}
}
